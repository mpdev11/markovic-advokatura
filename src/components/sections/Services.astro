---
import { services } from "../../data/navigation";

const basePath = import.meta.env.BASE_URL;
---

<section id="advokatske-usluge" class="py-24 sm:py-32 relative">
  <div class="mx-auto max-w-7xl px-6 lg:px-8 relative z-10">
    <div class="mx-auto max-w-2xl lg:text-center">
      <span class="subheading">Advokatske usluge</span>
      <h2 class="section-heading">Sve što Vam treba na jednom mestu</h2>
      <p class="mt-6 text-lg leading-8 text-gray-600">
          Svoj radim temeljim na predanosti, temeljnosti i želji da budem sigurna pravna podrška u svakoj situaciji.
      </p>
    </div>
    <div class="mx-auto mt-16 max-w-2xl sm:mt-20 lg:mt-24 lg:max-w-none">
      <div class="grid max-w-xl grid-cols-1 gap-x-8 gap-y-16 lg:max-w-none lg:grid-cols-3">
        {
          services.map((service) => (
            <a href={`${basePath}${service.path}`} class="flex flex-col p-8 rounded-2xl bg-background backdrop-blur-xl border border-white/40 shadow-[0_3px_15px_rgb(0,0,0,0.1)] transition-all duration-300 relative overflow-hidden group hover:shadow-[0_8px_25px_rgb(0,0,0,0.12)] hover:-translate-y-1">
              <div class="absolute inset-0 bg-gradient-to-br from-white/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300" />

              <!-- Icon -->
              <div class="flex justify-center mb-6 relative z-10">
                <div class="h-16 w-12 flex items-center justify-center text-primary [&>svg]:w-16 [&>svg]:h-16" set:html={service.icon} />
              </div>

              <!-- Heading -->
              <h3 class="text-xl font-semibold leading-7 text-gray-800 text-center mb-4 relative z-10 tracking-wide">
                {service.label}
              </h3>

              <!-- Description -->
              <p class="text-base leading-7 text-gray-600 text-center flex-auto relative z-10">
                {service.description}
              </p>

              <!-- Read More Button -->
              <div class="mt-6 text-center relative z-10">
                <span class="inline-flex items-center text-base font-semibold leading-6 text-primary group-hover:text-primary/80 transition-colors duration-200">
                  Saznajte više <span aria-hidden="true" class="ml-1">→</span>
                </span>
              </div>
            </a>
          ))
        }
      </div>
    </div>
  </div>
</section>
